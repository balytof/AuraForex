const { Client } = require('ssh2'); const conn = new Client(); conn.on('ready', () => { conn.exec('pm2 logs server --lines 1000 --nostream', (err, stream) => { if (err) throw err; let data = ''''; stream.on('close', () => { const lines = data.split('\n'); for(let i=0; i<lines.length; i++) { if(lines[i].includes('[PAMM] Erro') && lines[i].includes('Validation failed')) { console.log(lines.slice(i, i+25).join('\n')); break; } } conn.end(); }).on('data', (d) => data += d); }); }).connect({ host: '139.59.159.48', port: 22, username: 'root', password: '@Infomoi2023' });
